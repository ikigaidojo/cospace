<div id="pins" class="transition-enabled">
  <h1>Rooms</h1>
  <%- @rooms.each do |room| %>
    <div class="box panel panel-default">
       <div class="panel-body">
        <h2><%= room.name %></h2>
        <p>
          Submitted by 
          <%= room.description %>
        </p>
      </div>
    </div>
  <% end %>
  <p>lorem500</p>
</div>
