$("#available_rooms").replaceWith('<%= escape_javascript(generate_avaliable_rooms(@this_day)) %>');
// this file is run when instead of the usual view,
// genereate_availale_rooms is found as a function in the calendar_helper.rb